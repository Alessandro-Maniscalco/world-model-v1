# Roadmap Checklist

This checklist is the operational plan for implementing and validating `world-model-v1`. Each item is designed to be verifiable. Do not advance milestones until the definition of done is satisfied.

## Target hardware for validation

Use this as the default test baseline unless a task explicitly says otherwise.

* OS: Linux
* GPU: NVIDIA GeForce RTX 3080 (16 GB VRAM)
* Primary device: CUDA

## Milestone 0: Tooling, environment, and data sanity

### Repo and environment

* [x] Repository has `/docs/` with `scope.md`, `architecture.md`, and this checklist
* [x] `.gitignore` excludes cache, checkpoints, runs, and large artifacts
* [x] `requirements.txt` installs cleanly in a fresh venv on the target machine
* [x] CUDA is visible to PyTorch and basic GPU tensor ops run

### Dataset sanity

* [x] `scripts/check_dataset.py` loads `lerobot/libero` successfully
* [x] `LeRobotDataset(..., video_backend="pyav")` decodes frames successfully
* [x] Printed keys include:

  * [x] `observation.images.image`
  * [x] `action`
  * [x] `observation.state` (for proprio ablation)
* [x] Confirm 10 Hz alignment:

  * [x] timestamps are monotonic
  * [x] action and state exist for each sampled timestep

### VAE and caching sanity

* [x] `scripts/check_vae_roundtrip.py`:

  * [x] prints latent shape, min, max, mean, std
  * [x] saves side by side MP4 and PNG frames
* [x] `scripts/check_latent_cache.py`:

  * [x] writes cached latents for a small subset
  * [x] reloads latents and verifies exact file integrity
  * [x] confirms cached equals direct encoding using deterministic latents

### Masking sanity

* [x] `scripts/check_masking_leakage.py`:

  * [x] passes no leak test under the intended mask
  * [x] passes negative control test with mask disabled

Definition of done:

* [x] All scripts above run end to end without manual intervention and produce saved artifacts.

---

## Milestone 1: Latent time representation, chunking, and real masks

### Latent time API

* [x] Implement `src/world_model/latents/vae.py` with:

  * [x] `encode(video) -> latents`
  * [x] `decode(latents) -> video`
  * [x] deterministic mode for caching and training

### Latent time window split

* [x] Implement `src/world_model/data/pack.py` that outputs:

  * [x] `z_past` and `z_future` split in latent time after encoding
  * [x] `a_plan` aligned to the future target window
  * [x] optional `q` conditioning value(s) for proprio toggle

### Chunking K+1

* [x] Implement `src/world_model/chunking/schedule.py`:

  * [x] chunk boundaries defined in latent time
  * [x] produces `chunk_id` per token or per timestep for K+1 scheme
  * [x] supports K = 1 baseline

### Real attention masks

* [x] Implement `src/world_model/masking/block_causal.py`:

  * [x] block causal mask forbids attending to future chunks
  * [x] supports padding masks
  * [x] produces the exact attention mask format expected by the backbone

Definition of done:

* [x] Run `scripts/check_masking_leakage.py` using masks generated by `block_causal.py` and chunk ids generated by `schedule.py`.

---

## Milestone 2: Conditioning modules with AdaLN Zero

### Action encoder

* [x] Implement `src/world_model/conditioning/action_encoder.py`:

  * [x] maps `a_plan` to an AdaLN conditioning embedding (no action tokens in attention)
  * [x] hidden size matches DiT hidden size

### Proprio encoder

* [x] Implement `src/world_model/conditioning/proprio_encoder.py`:

  * [x] maps `q_l` to an AdaLN conditioning embedding (no proprio tokens in attention)
  * [x] can be toggled on or off through config

### AdaLN Zero

* [x] Implement `src/world_model/conditioning/adaln_zero.py`:

  * [x] produces scale and shift from conditioning embeddings
  * [x] zero initialization so blocks start near identity
  * [x] supports timestep embedding injection

Definition of done:

* [x] A unit test or script confirms that toggling proprio changes only the conditioning pathway and does not break shapes.

---

## Milestone 3: Wan DiT wrapper with correct interfaces

### Wan DiT integration

* [x] Implement `src/world_model/models/wan_dit_wrapper.py`:

  * [x] loads pretrained Wan DiT weights
  * [x] accepts:

    * [x] noisy future latent chunk
    * [x] teacher forced past clean chunks
    * [x] action conditioning embedding (from `a_plan`, via AdaLN only)
    * [x] optional proprio conditioning embedding (via AdaLN only)
    * [x] timestep t
    * [x] block causal attention mask
  * [x] outputs velocity field `u_theta` matching the noisy chunk shape

### Memory and throughput

* [x] Mixed precision enabled
* [x] Optional gradient checkpointing supported

Definition of done:

* [x] `scripts/check_forward_real_batch.py` verifies real-batch forward wiring with latent-time split, chunk ids, and block-causal mask.
* [x] Forward pass runs on a real batch with your intended l = 10, H = 8 pipeline on the RTX 3080 16 GB without out of memory.

---

## Milestone 4: Flow matching training with teacher forcing across chunks

### Milestone 4 implementation files

* [x] `src/world_model/training/flow_matching.py` (flow matching core + chunkwise loss)
* [x] `src/world_model/train/chunkwise_training.py` (batch train step, checkpointing, JSONL logging)
* [x] `src/world_model/train/__init__.py` (exports training orchestration helpers)
* [x] `scripts/train_world_model.py` (end-to-end training/overfit script)
* [x] `tests/test_flow_matching.py` (flow matching + chunkwise loss tests)
* [x] `tests/test_chunkwise_training.py` (train-step, checkpoint, logging tests)

### Flow matching core

* [x] Implement `src/world_model/training/flow_matching.py`:

  * [x] `sample_t`
  * [x] `make_noisy_and_target` returning `(z_t, v_target)`
  * [x] `w(t)` weighting function
  * [x] `chunkwise_teacher_forcing_loss` utility:
    * [x] iterates over chunk index k
    * [x] uses clean previous chunks `z_1^(1:k-1)` as teacher forced context
    * [x] applies mask so noisy chunk cannot attend to future chunks
    * [x] accumulates loss across k

### Chunkwise teacher forcing loop

* [x] Implement `scripts/train_world_model.py`:

  * [x] wires `chunkwise_teacher_forcing_loss` into the batch training loop
  * [x] runs optimizer/grad-step over real batches
  * [x] saves checkpoints and logs
  * [x] supports one-batch overfit mode (`--overfit-one-batch`) and qualitative artifacts (`--save-overfit-artifacts`)

### Overfit the real objective

* [x] Overfit test on one short episode or one batch:

  * [x] loss tracking reports start/end overfit loss on a fixed batch
  * [x] decoded prediction preview frames can be exported for visual comparison

Definition of done:

* [x] Real training objective tooling is in place to overfit a tiny subset and export decoded future-frame comparisons.

---

## Milestone 5: Evaluation and ablations

### Open loop rollout evaluation

* [ ] Implement `scripts/eval_rollout.py`:

  * [ ] rollout in latent space for multiple horizons
  * [ ] decode predicted latents to video for qualitative review
  * [ ] compute latent error metrics

### Proprio ablation

* [ ] Run evaluation with proprio enabled and disabled
* [ ] Produce a simple comparison artifact:

  * [ ] table or plot of metrics
  * [ ] representative videos

Definition of done:

* [ ] Clear recorded comparison for with proprio versus without proprio and a documented conclusion.

---

## Stretch milestones

### Second camera stream

* [ ] Add `observation.images.image2` support
* [ ] Decide tokenization strategy for multi camera conditioning

### Scaling

* [ ] Increase chunking K and horizon
* [ ] Expand beyond a single suite or dataset once stability is proven
